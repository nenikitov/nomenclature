[package]
name = "parser"
version = "0.1.0"
edition = "2024"

[dev-dependencies]
# TODO(nenikitov): Replace in favor of builtins `std::assert_matches` when `assert_matches` gets stabilized
assert_matches = "1.5.0"

[dependencies]
fortuples = "0.9.1"
